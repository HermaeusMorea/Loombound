#!/usr/bin/env bash
# Unified runtime entrypoint
#
# Usage:
#   ./run
#   ./run --slow deepseek --lang zh
#   ./run --campaign data/campaigns/my_campaign.json --slow deepseek

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/run.sh" "${@+"$@"}"
